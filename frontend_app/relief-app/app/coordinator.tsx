import { View, Text } from "react-native";

export default function Coordinator() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Coordinator Dashboard</Text>
    </View>
  );
}